{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "jsx": "react-native"
  },
  "include": ["**/*.ts", "**/*.tsx", "**/*.d.ts"],
  "exclude": ["node_modules", ".expo", "dist", "build"]
}
